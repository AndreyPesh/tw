export const enum EnumLinkPage {
  HOME = '/',
  PRODUCTS = '/products',
  CONTACTS = '/contacts',
  AUTH = '/auth',
  USER = '/user'
}

export const enum EnumTypeAuth {
  SIGNUP = 'signup',
  LOGIN = 'login',
}
