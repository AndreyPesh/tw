export const enum EnumLinkPage {
  HOME = '/',
  PRODUCTS = '/products',
  CONTACTS = '/contacts',
  AUTH = '/auth'
}

export const enum EnumTypeAuth {
  SIGNUP = 'signup',
  LOGIN = 'login',
}
